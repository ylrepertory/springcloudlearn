spring:
  application:
    name: service-center

server:
  port: 2002

eureka:
  client:
    healthcheck:
      enabled: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:2001/eureka/
  instance:
    preferIpAddress: true



